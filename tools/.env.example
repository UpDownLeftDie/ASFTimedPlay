# Server connection details
DEPLOY_USER=username         # Username for deployment server
DEPLOY_HOST=deploy.server.ip # IP or hostname of deployment server
DEPLOY_PATH=/path/to/plugins # Full path to plugins directory on deployment server

# Service restart details (SERVICE_USER and SERVICE_HOST are optional, default to DEPLOY values)
#SERVICE_USER=username         # Optional: Username for service host (defaults to DEPLOY_USER)
#SERVICE_HOST=service.server.ip # Optional: IP or hostname of service host (defaults to DEPLOY_HOST)
SERVICE_NAME=service_name # Docker container/service name to restart
DOCKER_SWARM=false        # Set to 'true' if using Docker Swarm mode
